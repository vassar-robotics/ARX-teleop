# ARX Control specific ignores

# Binary libraries (platform-specific, not tracked in git)
lib/**/*.so
lib/**/*.dylib
lib/**/*.dll

# But keep the directory structure
!lib/.gitkeep
!lib/arx_r5_python/.gitkeep
!lib/arx_r5_src/.gitkeep

# Python cache
__pycache__/
*.pyc